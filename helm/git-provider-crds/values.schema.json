{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "title": "Git Provider CRDs",
  "description": "Dedicated CRD chart for git-provider (Repo + LocalResource, group git.krateo.io). Golden rule: CRDs in a separate chart with a separate lifecycle. No tunable values.",
  "additionalProperties": false,
  "properties": {
    "global": {
      "type": "object",
      "title": "Global values"
    }
  }
}
